{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/githubnext/gh-aw-cao/main/.github/cao/cao.schema.json",
  "title": "Central Agentic Ops Policy",
  "description": "Versioned non-secret policy for Central Agentic Ops control and target repositories.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version"],
  "anyOf": [
    { "required": ["control-plane"] },
    { "required": ["target-authority"] }
  ],
  "allOf": [
    {
      "if": { "required": ["control-plane"] },
      "then": { "required": ["gh-aw-version"] }
    }
  ],
  "properties": {
    "$schema": {
      "const": "https://raw.githubusercontent.com/githubnext/gh-aw-cao/main/.github/cao/cao.schema.json"
    },
    "version": { "const": 1 },
    "gh-aw-version": {
      "type": "string",
      "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?$"
    },
    "control-plane": { "$ref": "#/$defs/controlPlane" },
    "target-authority": { "$ref": "#/$defs/targetAuthority" }
  },
  "$defs": {
    "owner": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9-]*$"
    },
    "repository": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9-]*/[A-Za-z0-9._-]+$"
    },
    "login": {
      "type": "string",
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9-]{0,37}[A-Za-z0-9])?$"
    },
    "mode": {
      "type": "string",
      "enum": ["review", "live"]
    },
    "maxRepositories": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1000
    },
    "rolloutPercent": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    },
    "monthlyAiCreditBudget": {
      "type": "integer",
      "minimum": 0
    },
    "octicon": {
      "type": "string",
      "enum": [
        "mark-github", "code", "repo", "server", "issue", "pull-request", "play", "eye",
        "shield", "meter", "graph", "codescan", "dependabot", "key", "beaker", "rocket",
        "workflow", "gear", "check-circle", "package", "external-link"
      ]
    },
    "workerPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["workflow"],
      "properties": {
        "workflow": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        },
        "enabled": { "type": "boolean" },
        "max-mode": { "$ref": "#/$defs/mode" }
      }
    },
    "targetPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode"],
      "properties": {
        "mode": { "$ref": "#/$defs/mode" }
      }
    },
    "packagePolicy": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "mode": { "$ref": "#/$defs/mode" },
        "max-repositories": { "$ref": "#/$defs/maxRepositories" },
        "rollout-percent": { "$ref": "#/$defs/rolloutPercent" },
        "monthly-ai-credit-budget": { "$ref": "#/$defs/monthlyAiCreditBudget" },
        "icon": { "$ref": "#/$defs/octicon" },
        "targets": {
          "type": "object",
          "propertyNames": { "$ref": "#/$defs/repository" },
          "additionalProperties": { "$ref": "#/$defs/targetPolicy" }
        },
        "workers": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          },
          "additionalProperties": { "$ref": "#/$defs/workerPolicy" }
        }
      },
      "additionalProperties": false
    },
    "controlPackages": {
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
      },
      "additionalProperties": { "$ref": "#/$defs/packagePolicy" }
    },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "allowed-owners": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/owner" }
        },
        "allowed-repositories": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/repository" }
        }
      }
    },
    "inventory": {
      "type": "object",
      "additionalProperties": false,
      "$comment": "The runtime validator also requires cell-index to be smaller than cell-count.",
      "properties": {
        "max-scan-repositories": { "type": "integer", "minimum": 1, "maximum": 100000 },
        "cell-count": { "type": "integer", "minimum": 1, "maximum": 1000 },
        "cell-index": { "type": "integer", "minimum": 0 },
        "batch-size": { "type": "integer", "minimum": 1, "maximum": 100000 },
        "batch-index": { "type": "integer", "minimum": 0 }
      }
    },
    "web": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "favicon": {
          "type": "string",
          "maxLength": 2048,
          "anyOf": [
            { "pattern": "^https://(?![^/?#]*@)[^\\s?#]+$" },
            { "pattern": "^(?!.*(?:^|/)\\.\\.(?:/|$))\\./[A-Za-z0-9][A-Za-z0-9._/-]*$" }
          ]
        }
      }
    },
    "defaults": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mode": { "$ref": "#/$defs/mode" },
        "max-repositories": { "$ref": "#/$defs/maxRepositories" },
        "rollout-percent": { "$ref": "#/$defs/rolloutPercent" },
        "monthly-ai-credit-budget": { "$ref": "#/$defs/monthlyAiCreditBudget" }
      }
    },
    "publishing": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "control-repositories": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/repository" }
        },
        "reviewers": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/login" }
        }
      },
      "if": {
        "required": ["enabled"],
        "properties": { "enabled": { "const": true } }
      },
      "then": {
        "required": ["reviewers"],
        "properties": { "reviewers": { "minItems": 1 } }
      }
    },
    "controlPlane": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "scope": { "$ref": "#/$defs/scope" },
        "inventory": { "$ref": "#/$defs/inventory" },
        "web": { "$ref": "#/$defs/web" },
        "defaults": { "$ref": "#/$defs/defaults" },
        "packages": { "$ref": "#/$defs/controlPackages" },
        "publishing": { "$ref": "#/$defs/publishing" }
      }
    },
    "targetPackage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["authority"],
      "properties": {
        "authority": { "$ref": "#/$defs/repository" }
      }
    },
    "targetPackages": {
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
      },
      "additionalProperties": { "$ref": "#/$defs/targetPackage" }
    },
    "targetAuthority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["packages"],
      "properties": {
        "packages": { "$ref": "#/$defs/targetPackages" }
      }
    }
  }
}