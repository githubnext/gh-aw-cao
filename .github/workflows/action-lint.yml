name: GitHub Actions lint

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/**"
  pull_request:
    paths:
      - ".github/workflows/**"

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      failed: ${{ steps.result.outputs.failed }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run actionlint and zizmor
        id: lint
        continue-on-error: true
        shell: bash
        run: |
          mapfile -d '' files < <(
            find .github/workflows .github/actions -type f \
              \( -name '*.yml' -o -name '*.yaml' \) \
              ! -name '*.lock.yml' -print0 | sort -z
          )

          set +e
          set -o pipefail
          docker run --rm --network none --cap-drop ALL \
            --security-opt no-new-privileges \
            --volume "$PWD:/repo:ro" --workdir /repo \
            rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 \
            "${files[@]}" 2>&1 | tee /tmp/actionlint.txt
          actionlint_status=${PIPESTATUS[0]}

          docker run --rm --network none --cap-drop ALL \
            --security-opt no-new-privileges \
            --volume "$PWD:/repo:ro" --workdir /repo \
            ghcr.io/zizmorcore/zizmor:1.30.1@sha256:a2eb396d886c053073405c7a980f2139ba2248ec172243cfa3841e57196e8101 \
            --format plain "${files[@]}" 2>&1 | tee /tmp/zizmor.txt
          zizmor_status=${PIPESTATUS[0]}

          if (( actionlint_status != 0 || zizmor_status != 0 )); then
            exit 1
          fi

      - name: Record lint result
        id: result
        if: always()
        env:
          LINT_FAILED: ${{ steps.lint.outcome == 'failure' }}
        run: echo "failed=$LINT_FAILED" >> "$GITHUB_OUTPUT"

      - name: Upload lint output
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: github-actions-lint
          path: |
            /tmp/actionlint.txt
            /tmp/zizmor.txt
          if-no-files-found: warn
          retention-days: 1

      - name: Report lint problems
        if: steps.lint.outcome == 'failure'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const fs = require('fs')
            for (const [tool, path] of [
              ['actionlint', '/tmp/actionlint.txt'],
              ['zizmor', '/tmp/zizmor.txt'],
            ]) {
              let output = 'No output was captured.'
              try {
                output = fs.readFileSync(path, 'utf8')
              } catch {}
              core.info(`${tool} output:\n${output}`)
            }
            core.setFailed('actionlint or zizmor found problems.')

  report:
    if: always() && needs.lint.result != 'cancelled'
    needs: lint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: read
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Download lint output
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: github-actions-lint
          path: lint-output

      - name: Update lint issue
        if: github.event_name == 'push'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          LINT_FAILED: ${{ needs.lint.outputs.failed }}
        with:
          script: |
            const fs = require('fs')
            const title = 'GitHub Actions lint failures on main'
            const { owner, repo } = context.repo
            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner,
              repo,
              state: 'open',
              per_page: 100,
            })

            for (const issue of issues.filter((candidate) =>
              !candidate.pull_request && candidate.title === title
            )) {
              await github.rest.issues.update({
                owner,
                repo,
                issue_number: issue.number,
                state: 'closed',
                state_reason: 'not_planned',
              })
            }

            if (process.env.LINT_FAILED !== 'true') {
              return
            }

            const escapeHtml = (value) => value
              .replaceAll('&', '&amp;')
              .replaceAll('<', '&lt;')
              .replaceAll('>', '&gt;')
            const output = (path) => {
              try {
                return escapeHtml(fs.readFileSync(`lint-output/${path}`, 'utf8').slice(0, 25000))
              } catch {
                return 'No output was captured.'
              }
            }
            const runUrl = `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`
            const body = [
              'actionlint or zizmor found problems in GitHub Actions files on `main`.',
              '',
              `Workflow run: ${runUrl}`,
              '',
              'Generated `.lock.yml` files were excluded.',
              '',
              '<details><summary>actionlint output</summary><pre>',
              output('actionlint.txt'),
              '</pre></details>',
              '',
              '<details><summary>zizmor output</summary><pre>',
              output('zizmor.txt'),
              '</pre></details>',
            ].join('\n')

            await github.rest.issues.create({ owner, repo, title, body })

      - name: Update pull request comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          LINT_FAILED: ${{ needs.lint.outputs.failed }}
        with:
          script: |
            const fs = require('fs')
            const marker = '<!-- github-actions-lint -->'
            const { owner, repo } = context.repo
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number: context.issue.number,
              per_page: 100,
            })
            const existing = comments.find((comment) =>
              comment.user?.type === 'Bot' && comment.body?.includes(marker)
            )

            if (process.env.LINT_FAILED !== 'true') {
              if (existing) {
                await github.rest.issues.deleteComment({
                  owner,
                  repo,
                  comment_id: existing.id,
                })
              }
              return
            }

            const escapeHtml = (value) => value
              .replaceAll('&', '&amp;')
              .replaceAll('<', '&lt;')
              .replaceAll('>', '&gt;')
            const output = (path) => {
              try {
                return escapeHtml(fs.readFileSync(`lint-output/${path}`, 'utf8').slice(0, 25000))
              } catch {
                return 'No output was captured.'
              }
            }
            const runUrl = `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`
            const body = [
              marker,
              '## GitHub Actions lint failures',
              '',
              `Workflow run: ${runUrl}`,
              '',
              'Generated `.lock.yml` files were excluded.',
              '',
              '<details><summary>actionlint output</summary><pre>',
              output('actionlint.txt'),
              '</pre></details>',
              '',
              '<details><summary>zizmor output</summary><pre>',
              output('zizmor.txt'),
              '</pre></details>',
            ].join('\n')

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body,
              })
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: context.issue.number,
                body,
              })
            }
