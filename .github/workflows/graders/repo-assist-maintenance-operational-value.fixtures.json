[
  {
    "name": "attained",
    "request": {
      "schemaVersion": 1,
      "run": {"id": "1", "repository": "githubnext/gh-aw-cao", "workflow": "Repo Assist / Maintenance"},
      "event": {"inputs": {"target_repo": "octo/service", "safe_output_repo": "octo/control", "safe_output_mode": "live"}},
      "outputs": [{"type": "create_pull_request", "repo": "octo/service", "title": "Avoid duplicate configuration reads", "branch": "repo-assist/improve-config-read", "body": "The parser now reads configuration once.\n\n**Action:** Review and merge after CI remains green.\n\nEvidence: profiling showed duplicate reads.\nRationale: remove repeated I/O without changing behavior.\nValidation: focused tests pass.\n<!-- repo-assist:maintenance target=octo/service work=config-read -->"}],
      "config": {}
    },
    "expected": [{"id": "decision-ready-maintenance-patch", "value": 1}]
  },
  {
    "name": "missed",
    "request": {
      "schemaVersion": 1,
      "run": {"id": "2", "repository": "githubnext/gh-aw-cao", "workflow": "Repo Assist / Maintenance"},
      "event": {"inputs": {"target_repo": "octo/service", "safe_output_repo": "octo/control", "safe_output_mode": "review"}},
      "outputs": [{"type": "create_issue", "repo": "octo/other", "title": "octo/service maintenance config-read review", "body": "**Action:** Review.\nEvidence: duplicate reads.\nRationale: remove I/O.\nValidation: tests pass.\n<!-- repo-assist:maintenance target=octo/service work=config-read -->"}],
      "config": {}
    },
    "expected": [{"id": "decision-ready-maintenance-patch", "value": 0}]
  },
  {
    "name": "unavailable",
    "request": {"schemaVersion": 1, "run": {"id": "3"}, "event": {"inputs": {}}, "outputs": [], "config": {}},
    "expected": [{"id": "decision-ready-maintenance-patch", "value": null}]
  },
  {
    "name": "malformed",
    "request": {"schemaVersion": 2, "run": {}, "event": {}, "outputs": "invalid", "config": {}},
    "expected": [{"id": "decision-ready-maintenance-patch", "value": null}]
  },
  {
    "name": "noop",
    "request": {
      "schemaVersion": 1,
      "run": {"id": "4"},
      "event": {"inputs": {"target_repo": "octo/service", "safe_output_mode": "review"}},
      "outputs": [{"type": "noop", "message": "No low-risk improvement met the evidence threshold."}],
      "config": {}
    },
    "expected": [{"id": "decision-ready-maintenance-patch", "value": null}]
  }
]