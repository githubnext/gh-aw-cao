name: CAO Dashboard
description: Root-package component that deploys an access-controlled, read-only Central Agentic Ops report with GitHub Pages and optional background data updates; not a standalone installation entry point.
min-version: v0.89.20
experimental: true
includes:
  - .github/workflows/cao-dashboard.yml
