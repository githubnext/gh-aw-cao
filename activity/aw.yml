name: CAO Activity
description: Root-package component that maintains a shared, bounded snapshot of GitHub Agentic Workflow activity; not a standalone installation entry point.
min-version: v0.89.20
experimental: true
includes:
  - .github/workflows/cao-activity.yml
